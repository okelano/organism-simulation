# Django framework for building the web application
Django==4.2.4

# PostgreSQL database adapter for Python, used to interface Django with PostgreSQL
psycopg2-binary==2.9.8

# Django REST Framework for building RESTful APIs
djangorestframework==3.14.0

# NumPy for numerical computations and handling large arrays
numpy==1.25.2

# Pandas for data manipulation and analysis
pandas==2.1.0

# Matplotlib for plotting and visualization
matplotlib==3.7.1

# Scikit-learn for machine learning algorithms
scikit-learn==1.3.1

# Pytest for unit testing
pytest==7.4.0

# Coverage for measuring code coverage in tests
coverage==7.3.1

# Requests for making HTTP requests
requests==2.30.0

# BeautifulSoup for parsing HTML and XML documents
beautifulsoup4==4.12.2

# Flask for lightweight web services (if needed for microservices)
flask==2.3.2

# Celery for asynchronous task queues
celery==5.3.0

# Redis as a broker for Celery (optional, based on your task queue needs)
redis==4.7.0

# Ansible for configuration management and automated testing
ansible==8.1.0

# Docker Python SDK for managing Docker containers programmatically
docker==6.1.3

# PyYAML for parsing YAML files, useful for Ansible playbooks
pyyaml==6.0

# OpenCV for image processing (if needed for image-based simulations)
opencv-python==4.7.0.72

# SciPy for additional scientific computing functionalities
scipy==1.11.2

# Jupyter Notebook for interactive computing (optional for notebooks)
jupyter==1.0.0

# SQLAlchemy for SQL toolkit and Object-Relational Mapping (if needed)
sqlalchemy==2.0.15

# Django Extensions for additional Django management commands and features
django-extensions==3.2.0

# Django Debug Toolbar for debugging Django applications
django-debug-toolbar==4.0.2

# Pillow for image processing and manipulation
Pillow==9.5.0

# SQLAlchemy for SQL toolkit and Object-Relational Mapping (if needed)
sqlalchemy==2.0.15

# External requirements for the system

django-celery-beat==2.5.0
django-celery-results==2.4.0
gunicorn==20.1.0
## r bioconductor with possibilities to use R for python
rpy2==3.5
